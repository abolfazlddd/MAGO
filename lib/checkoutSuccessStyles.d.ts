import type React from "react";

export const successStyles: {
  container: React.CSSProperties;
  orderIdCode: React.CSSProperties;
  neutralButton: React.CSSProperties;
  nextStepsBox: React.CSSProperties;
  stepsList: React.CSSProperties;
  subtleCode: React.CSSProperties;
  primaryButton: React.CSSProperties;
};
